#include "main.h"
/**
 * mul - function that multiplies intergers.
 * @a: function parameter
 * @b: function parameter
 *
 * Return: results
 */
int mul(int a, int b)
{
	int results;

	results = a * b;
	return (results);
}
