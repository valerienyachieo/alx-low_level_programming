#include <stdio.h>
#include <stdlib.h>
/**
 * main - function that printsname of a program
 * @argc: count of arguments
 * @argv: array of pointers of arguments
 *
 * Return: always 0 (sucess)
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
